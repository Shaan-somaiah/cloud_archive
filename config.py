zpools = {
    "dump/main": [
        "common",
        "kube_vol"
    ]
}

kopia = "/usr/bin/kopia"
rclone = "/usr/bin/rclone"
zfs = "/usr/sbin/zfs"

snapshot_name_prefix = "kopia_managed"
clone_name_prefix = "clone_kopia_managed"
